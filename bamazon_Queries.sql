SELECT item_ID as Item, product_name as Product, department_name as Department, product_price as Price, stock_qty as Quantity
	FROM bamazon_db.products;
    
SELECT * FROM products;

-- UPDATE products SET stock_qty = stock_qty + 4 WHERE item_ID = 8;

-- SELECT * FROM products;